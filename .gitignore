out/
*.img
*.iso
*.ppm
cores/
__pycache__/
assets/

# ⚠ Optional. Drop public keys here and every
# image built afterwards lets you straight in over ssh. Gitignored because it is
# a specific person's key, not because a public key is a secret — each person
# supplies their own.
installer/authorized_keys
