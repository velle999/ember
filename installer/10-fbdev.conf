# The firmware framebuffer is the display here (vc4 KMS never registers a
# device on this kernel), so X runs on fbdev — which has no RandR, meaning
# resolution lives in /boot/config.txt and rotation lives right here.
Section "Device"
    Identifier "fb"
    Driver "fbdev"
    Option "fbdev" "/dev/fb0"
EndSection
