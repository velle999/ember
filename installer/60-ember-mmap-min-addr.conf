# Mac OS X i386 executables are non-PIE with __TEXT at 0x1000, below Void's
# 65536 floor, so a Mach-O loader (Halo's Mac build) cannot map them there.
# 4096 is the kernel's own default and still keeps page zero unmappable.
vm.mmap_min_addr = 4096
